# ============================================
# Prompt Library - Environment Configuration
# ============================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# ============================================
# Database Configuration
# ============================================
# PostgreSQL credentials for the database service
POSTGRES_USER=promptlib
POSTGRES_PASSWORD=promptlib
POSTGRES_DB=promptlib

# ============================================
# Google OAuth 2.0 Configuration
# ============================================
# Follow these steps to obtain credentials:
#
# 1. Go to Google Cloud Console: https://console.cloud.google.com/
#
# 2. Create a new project or select an existing one:
#    - Click the project dropdown at the top of the page
#    - Click "New Project", enter a name, and create it
#
# 3. Enable the Google+ API (required for profile info):
#    - Go to "APIs & Services" > "Library"
#    - Search for "Google+ API" and enable it
#
# 4. Configure OAuth consent screen:
#    - Go to "APIs & Services" > "OAuth consent screen"
#    - Select "External" user type (or Internal for Google Workspace)
#    - Fill in the required fields:
#      - App name: "Prompt Library" (or your preferred name)
#      - User support email: Your email
#      - Developer contact email: Your email
#    - Add scopes: email, profile, openid
#    - Add test users if in testing mode
#
# 5. Create OAuth credentials:
#    - Go to "APIs & Services" > "Credentials"
#    - Click "Create Credentials" > "OAuth client ID"
#    - Select "Web application" as the application type
#    - Name: "Prompt Library Web Client"
#    - Authorized JavaScript origins: http://localhost
#    - Authorized redirect URIs: http://localhost/auth/google/callback
#    - Click "Create"
#
# 6. Copy the Client ID and Client Secret below

GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# OAuth redirect URI (must match Google Cloud Console settings)
GOOGLE_REDIRECT_URI=http://localhost/auth/google/callback

# ============================================
# Security Configuration
# ============================================
# JWT secret key for signing authentication tokens
# Generate a secure random string for production:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=change-me-in-production

# ============================================
# Application Configuration
# ============================================
# CORS allowed origins (comma-separated for multiple)
CORS_ORIGINS=http://localhost:80,http://localhost

# Host port for the application (nginx will listen on this port)
HOST_PORT=80

# Frontend API URL (used by the frontend to make API calls)
VITE_API_URL=http://localhost:80
